# Resume — Alpha & Data insights + Web2 narration build

**Stopped:** 2026-06-01 (Claude usage limit), clean checkpoint.
**Branch:** `alpha-data-insights-narration` (off `master`, local only — NOT pushed).
**Plan:** `docs/superpowers/plans/2026-06-01-alpha-data-insights-and-narration.md` (14 tasks).
**Spec:** `docs/superpowers/specs/2026-06-01-alpha-data-insights-and-narration-design.md`.

## Where we are

Executing the plan via **subagent-driven development** (fresh implementer subagent per task + review). Done so far:

| Task | What | Commit |
|------|------|--------|
| 1 | Lint debt cleared (10 errs + 6 warns) | `ae110d4` + fix `b49850a` |
| 2 | vitest added | `050a69f` |
| 3 | `lib/labels.ts` (Web2 labels, TDD) | `c7ec92a` |
| 4 | `lib/insights.ts` (findings math, TDD, 13 tests green) | `45d57b1` |
| 5 | `useSmartMoneyBands` hook (mock + live top-8) | `d25c52c` |

HEAD = `d25c52c`. `pnpm --filter frontend lint` clean, `build` green, `test` green (labels+insights).

## NEXT: resume at Task 6

Re-invoke subagent-driven execution starting at **Task 6**. Remaining: **6,7,8,9,10,11,12,13,14**. Read the plan file for each task's full verbatim text + code (don't make subagents read it — paste the task text into each implementer prompt, per the skill).

Quick resume line:
> "Resume executing `docs/superpowers/plans/2026-06-01-alpha-data-insights-and-narration.md` with subagent-driven-development, starting at Task 6. Tasks 1–5 are done (HEAD d25c52c)."

### Remaining tasks (pointers — full code is in the plan)
- **6 — `/insights` route shell.** page.tsx + InsightsClient.tsx + AppHeader nav link. Has a temporary `FindingsContext` placeholder REMOVED in Task 7. (Implementer dispatch was the call interrupted at stop — nothing was written; start fresh.)
- **7 — Smart-money centerpiece + consensus band chart.** `SmartMoneyCard.tsx` (bullet) + `ConsensusBandCard.tsx` (Recharts Area+Line + uncertainty badge). Replaces the Task-6 placeholder.
- **8 — Notable-move + top-performers cards + a11y.** `NotableMoveCard.tsx`, `TopPerformersCard.tsx`, data-table fallback toggle, loading skeleton/empty states.
- **9 — Landing teaser.** `components/landing/InsightTeaser.tsx` + new `StoryFrame` in `app/page.tsx`.
- **10 — Reasoner self-narration (agents).** Extend `agents/claude-reasoner/src/forecast.ts` (pure `parseForecastText`, optional `summary`+`confidence_rationale`), `prompt.ts` output contract, 6 `fewshot/*.json`. TDD via vitest in the reasoner pkg.
- **11 — `/api/narrate` route.** `lib/narrate.ts` (TDD, injected-fetcher + cache + fallback) + `app/api/narrate/route.ts` (server-only `OPENROUTER_API_KEY`) + `.env.example`.
- **12 — Narration render on `/agent/[id]`.** `components/app/ForecastSummary.tsx` + render in `FeaturedReasoning` + every prediction row; extend mock `ReasoningTrace` with `summary`/`confidenceRationale`; make ARIMA rows expandable.
- **13 — Playwright tour + 375px smoke.** `@playwright/test` + config + `e2e/tour.spec.ts` + `e2e/responsive.spec.ts`. Browser install may be blocked → manual checklist fallback (plan Task 13 Step 7).
- **14 — Verification gate + CLAUDE.md entry.** Full lint/test/build/reasoner-tsc sweep; append a dated §6 session entry to `CLAUDE.md`; then offer finishing-a-development-branch (local FF-merge to master vs PR — user decides; deployment still deferred).

## Execution model (keep consistent)
- **Implementers:** `general-purpose`, model **sonnet** (plan is detailed; sonnet succeeds). Paste full task text + scene-setting context into each prompt.
- **Review:** for **pure-module / tooling tasks** (already done for 2–5) a direct read-verify is fine to conserve dispatch budget. For **integration/UI tasks (6,7,8,12) and the agent change (10)**, do the **two-stage subagent review** (spec compliance, then code quality) per the skill.
- Commit after each task; commit messages MUST end with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Continuous execution — don't pause between tasks except on BLOCKED.

## Caveats baked into the plan (don't re-litigate)
- Numbers are **demo-shaped** until a live indexer is hosted (deployment deferred to a separate later conversation).
- "Rising agents" → reframed as **top performers** (momentum needs history we lack from one snapshot).
- `/api/narrate` cache is **in-memory per server instance**; key is server-only `OPENROUTER_API_KEY` (never `NEXT_PUBLIC`).
- Playwright browser install on Windows may fail → fallback manual checklist.
- Deferred (NOT this branch): Vercel deploy, contract verification (Etherscan V2), Railway indexer hosting, ≥2-min demo video, submission-text reframe to Alpha & Data primary.

## Strategy decisions locked (context)
Primary track = **Alpha & Data** (Mirana); secondary = AI x RWA. Awards chased: Alpha&Data + Best UI/UX + 20-Project-Deployment. Web2-legibility is a hard requirement on all new surfaces.
